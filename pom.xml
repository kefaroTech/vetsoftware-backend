<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
             https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.1.0</version>
        <relativePath/>
    </parent>

    <groupId>com.vetsoftware</groupId>
    <artifactId>vetsoftware</artifactId>
    <version>2.2.0-dev.1</version>
    <name>VetSoftware</name>

    <properties>
        <java.version>25</java.version>
        <argLine></argLine>
        <!-- Mismo motivo que argLine: la reescribe jacoco:prepare-agent-integration, y
             declararla vacia evita que @{failsafeArgLine} quede literal si jacoco no corre. -->
        <failsafeArgLine></failsafeArgLine>
        <aws.sdk.version>2.49.3</aws.sdk.version>
        <datasource-micrometer.version>2.2.1</datasource-micrometer.version>
        <opentelemetry.instrumentation.version>2.28.1-alpha</opentelemetry.instrumentation.version>
        <bucket4j.version>8.19.0</bucket4j.version>
        <jjwt.version>0.13.0</jjwt.version>
        <openhtmltopdf.version>1.1.65</openhtmltopdf.version>
        <zxing.version>3.5.4</zxing.version>
        <spotless-maven-plugin.version>3.6.0</spotless-maven-plugin.version>
        <eclipse-jdt-formatter.version>4.39</eclipse-jdt-formatter.version>
        <maven-checkstyle-plugin.version>3.6.0</maven-checkstyle-plugin.version>
        <checkstyle.version>13.9.0</checkstyle.version>
        <archunit.version>1.4.1</archunit.version>
        <jacoco-maven-plugin.version>0.8.14</jacoco-maven-plugin.version>
        <maven-enforcer-plugin.version>3.6.3</maven-enforcer-plugin.version>
        <cyclonedx-maven-plugin.version>2.9.3</cyclonedx-maven-plugin.version>

        <!-- BE-27. Sobrescriben lo que gestiona spring-boot-dependencies 4.1.0 (netty
             4.2.15.Final, httpcore5 5.4.2). El primer escaneo del SBOM encontro 7
             vulnerabilidades HIGH con correccion publicada en ese arbol:

               netty  4.2.15.Final -> CVE-2026-59901 (bucle infinito en bzip2),
                                      CVE-2026-55831, CVE-2026-55833, CVE-2026-56745
                                      (denegacion de servicio en SPDY),
                                      CVE-2026-56819 (fuga de memoria en HTTP/2)
               httpcore5 5.4.2     -> CVE-2026-54399, CVE-2026-54428 (denegacion de
                                      servicio por cabeceras HTTP y HPACK)

             Netty entra por Lettuce (Redis) y httpcore5 por el cliente HTTP: los dos
             viajan dentro del jar desplegado.

             OJO AL CADUCAR: una version fijada aqui GANA a la del parent. Cuando
             Spring Boot publique una version que ya incluya estas correcciones, estas
             dos lineas pasan de proteger a frenar, y lo haran en silencio porque
             Dependabot no ve una propiedad que no respalda ninguna declaracion de
             dependencia. Se revisan al subir de version el parent y se borran en
             cuanto la gestionada iguale o supere a esta. -->
        <netty.version>4.2.17.Final</netty.version>
        <httpcore5.version>5.4.3</httpcore5.version>
        <!-- Suelo de cobertura global: solo puede SUBIR. Medido 98,83 % el 2026-08-17 tras
             la campana BE-COV; se fija un par de decimas por debajo para dejar margen a
             refactors que muevan el denominador. Ver "Cobertura" en CLAUDE.md.

             Estuvo un rato en 0.90 como objetivo declarado, con el build rojo a
             proposito, y se devolvio al valor medido: un umbral que nadie puede poner en
             verde no protege nada y acaba desactivandose. Ahora 0.98 SI es alcanzable,
             porque es lo que mide la suite de verdad.

             HISTORICO DE LO MEDIDO. Cada linea es la medicion de SU fecha y se deja tal
             como se tomo: son fotos, no el estado de hoy. La ultima es la vigente.
               12,73 % (2026-08-08)
               14,24 % (2026-08-08, modulo animal)
               25,64 % (2026-08-09, +17 features)
               33,44 % (2026-08-12, campana de cobertura + rodajas de persistencia)
               53,15 % (2026-08-16, BE-10)
               98,83 % (2026-08-17, BE-COV: 10.771 unitarias + 1.130 de integracion)
               98,36 % (2026-08-22, arbol 1.8.0-dev.1: 26.352/26.791 lineas, con
                        10.183 unitarias -10.154 de `default-test` + 29 de
                        `archunit-tests`- y 1.069 de integracion, cero fallos)

             OJO AL COMPARAR LA PENULTIMA CON LA ULTIMA: 98,83 -> 98,36 no es cobertura
             perdida sino denominador movido (28.214 lineas medidas entonces, 26.791
             ahora). El suelo se queda en 0.98 porque las dos lo superan; el trinquete no
             sube al 0,9836 justamente porque ese decimal depende del denominador.

             BAJADA DELIBERADA A 0.90 (2026-08-24), decidida por el usuario. El repositorio
             exige justificar por escrito toda bajada del trinquete, asi que aqui esta:

             El modelo de suscripciones anadio diez slices verticales y 26 tablas de una sola
             vez -catalogo, precios, configurador, cotizaciones, contratos, entitlements y la
             capa de dinero- y elimino el concepto de membresia. La medicion real tras esa
             entrega es 0,9393 (31.374/33.400 lineas), con el deficit concentrado en lo nuevo:
             subscription 0,73 - configurator 0,51 - subscriptionbilling 0,75 - quote 0,82 -
             entitlement 0,77. Los slices preexistentes siguen altos.

             OJO, Y ESTO ES LO QUE HAY QUE VIGILAR: 0.90 queda POR DEBAJO del 0,9393 medido,
             asi que el suelo deja tres puntos de holgura y la cobertura puede degradarse sin
             que el build lo note. Eso es exactamente lo que el trinquete existe para impedir.
             Es una concesion temporal mientras se escriben los tests de los slices nuevos, no
             el nivel al que este proyecto aspira.

             COMO SE DESHACE: cuando la suite de los slices nuevos este escrita, subir el suelo
             al valor medido y estable, y volver a la regla de que solo puede subir. El valor
             anterior era 0.98 y lo sostenia una suite de 10.771 tests unitarios y 1.130 de
             integracion; hoy son 11.022 y 1.082 sobre un denominador mucho mayor. -->
        <jacoco.line.minimum>0.90</jacoco.line.minimum>

        <!-- LOS DOS INTERRUPTORES QUE PERMITEN PARTIR `mvn verify` EN VARIOS JOBS DE CI.
             Los dos valen `false`, asi que un `mvn verify` local o de cualquier otro
             workflow se comporta EXACTAMENTE igual que antes de existir. Solo `ci.yml` los
             pone en true, y cada uno en un solo job.

             skipUTs — el gemelo de `-DskipITs` que surefire no trae. Failsafe si publica
             `skipITs`, asi que el job de unitarias se limita a `mvn package` y ya se salta
             las *IT por fase; el job de integracion, en cambio, TIENE que llegar a `verify`
             -es `failsafe:verify` quien convierte un fallo de *IT en build rojo, no
             `failsafe:integration-test`- y por el camino pasa por la fase `test`. Sin este
             interruptor ese job volveria a ejecutar las 10.183 unitarias (medidas el
             2026-08-22 sobre este arbol, contando elementos <testcase>), que es justo lo
             que el reparto viene a evitar.

             OJO A CUAL DE LOS DOS PARAMETROS DE SUREFIRE SE CABLEA, que no es indiferente:
             va sobre `skip` (propiedad `maven.test.skip`) y NO sobre `skipTests`. Un
             parametro configurado en el POM gana a la propiedad de usuario del mismo
             nombre, asi que cablear `skipTests` habria dejado `-DskipTests` sin efecto — y
             `-DskipTests` es lo que usa el camino solo-documentacion de `ci.yml` para
             empaquetar el jar sin correr nada. Habria pasado de empaquetar en segundos a
             ejecutar la suite entera, en el unico camino que existe para no ejecutarla.
             Contrapartida asumida: `-Dmaven.test.skip=true` deja de silenciar surefire (el
             compilador si se salta los tests, y surefire encuentra 0 clases y pasa).

             skipCoverageCheck — apaga SOLO la ejecucion `check-floor`. El suelo se mide
             sobre la fusion de las dos coberturas, asi que en un job que solo tiene una
             mitad la comprobacion no es que sobre: es que MIENTE. En el job de integracion
             mediria integracion sola y daria rojo por un motivo falso; el sitio donde se
             mide de verdad es el job `coverage-gate`, con los dos .exec ya descargados.
             Se cablea por ejecucion y no con el `jacoco.skip` del plugin porque ese es
             unico para todos los goals y se llevaria por delante a `prepare-agent`, es
             decir la cobertura entera. -->
        <skipUTs>false</skipUTs>
        <skipCoverageCheck>false</skipCoverageCheck>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>software.amazon.awssdk</groupId>
                <artifactId>bom</artifactId>
                <version>${aws.sdk.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>net.ttddyy.observation</groupId>
                <artifactId>datasource-micrometer-bom</artifactId>
                <version>${datasource-micrometer.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-webmvc</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-restclient</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-cache</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-redis</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-aspectj</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-thymeleaf</artifactId>
        </dependency>
        <dependency>
            <groupId>io.github.openhtmltopdf</groupId>
            <artifactId>openhtmltopdf-pdfbox</artifactId>
            <version>${openhtmltopdf.version}</version>
        </dependency>

        <!-- Persistencia -->
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-liquibase</artifactId>
        </dependency>

        <!-- JWT -->
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-api</artifactId>
            <version>${jjwt.version}</version>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-impl</artifactId>
            <version>${jjwt.version}</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-jackson</artifactId>
            <version>${jjwt.version}</version>
            <scope>runtime</scope>
        </dependency>

        <!-- AWS -->
        <dependency>
            <groupId>software.amazon.awssdk</groupId>
            <artifactId>s3</artifactId>
        </dependency>

        <!-- #111. Aqui vivia spring-boot-starter-mail, "envio de correo (F4)". Nunca
             se uso: cero referencias a org.springframework.mail, jakarta.mail,
             JavaMailSender o MimeMessage, y ni una propiedad spring.mail.*. Todo el
             correo sale por HTTP contra Resend. Lo que arrastraba al jar
             -spring-boot-mail, jakarta.mail-api, angus-mail, angus-activation,
             jakarta.activation-api- entraba tambien al SBOM de CycloneDX y lo
             escaneaba Trivy, asi que cada CVE de SMTP habia que triarlo sobre codigo
             que jamas se ejecuta. La ejecucion analyze-only de mas abajo es lo que
             impide que vuelva a pasar. -->

        <!-- Generación de códigos QR (CUFE/CUDE + URL DIAN en el PDF) -->
        <dependency>
            <groupId>com.google.zxing</groupId>
            <artifactId>core</artifactId>
            <version>${zxing.version}</version>
        </dependency>
        <dependency>
            <groupId>com.google.zxing</groupId>
            <artifactId>javase</artifactId>
            <version>${zxing.version}</version>
        </dependency>

        <!-- Rate limiting -->
        <dependency>
            <groupId>com.bucket4j</groupId>
            <artifactId>bucket4j_jdk17-redis-common</artifactId>
            <version>${bucket4j.version}</version>
        </dependency>
        <!-- Rate limiting distribuido sobre Redis (buckets compartidos entre réplicas + TTL) -->
        <dependency>
            <groupId>com.bucket4j</groupId>
            <artifactId>bucket4j_jdk17-lettuce</artifactId>
            <version>${bucket4j.version}</version>
        </dependency>

        <!-- Spring Security: method security (@PreAuthorize) + BCrypt -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>

        <!-- API documentation -->
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
            <version>3.0.3</version>
        </dependency>

        <!-- Observabilidad: métricas + tracing -->
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-registry-prometheus</artifactId>
        </dependency>
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-registry-otlp</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-opentelemetry</artifactId>
        </dependency>
        <dependency>
            <groupId>io.opentelemetry.instrumentation</groupId>
            <artifactId>opentelemetry-logback-appender-1.0</artifactId>
            <version>${opentelemetry.instrumentation.version}</version>
        </dependency>

        <!-- JDBC tracing con convenciones semánticas estables de OpenTelemetry -->
        <dependency>
            <groupId>net.ttddyy.observation</groupId>
            <artifactId>datasource-micrometer-spring-boot</artifactId>
        </dependency>
        <dependency>
            <groupId>net.ttddyy.observation</groupId>
            <artifactId>datasource-micrometer-opentelemetry</artifactId>
        </dependency>

        <!-- Test -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-webmvc-test</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- ArchUnit: las reglas de CLAUDE.md como test que rompe el build.
             Se declaran api + engine en lugar del agregador archunit-junit5
             porque es lo mismo que él agrupa, sin el pom intermedio. -->
        <!-- @DataJpaTest contra MySQL real. H2 no sirve aqui: el schema depende de
             comportamientos concretos de MySQL (TINYINT pelado, AUTO_INCREMENT con
             IDENTITY, SELECT ... FOR UPDATE), y un motor distinto daria verde sobre algo
             que no es lo que se despliega. Versiones gestionadas por el BOM de Boot. -->
        <!-- Boot 4 partio las rodajas de test por modulo: @DataJpaTest ya no viene en
             spring-boot-starter-test, hay que pedir su starter. Lo mismo @WebMvcTest. -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-testcontainers</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>testcontainers-junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>testcontainers-mysql</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.tngtech.archunit</groupId>
            <artifactId>archunit-junit5-api</artifactId>
            <version>${archunit.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.tngtech.archunit</groupId>
            <artifactId>archunit-junit5-engine</artifactId>
            <version>${archunit.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
                <executions>
                    <execution>
                        <goals>
                            <goal>properties</goal>
                        </goals>
                    </execution>
                    <!-- #111. La causa raiz de que spring-boot-starter-mail viviera meses
                         en el jar y en el SBOM sin una sola linea de codigo detras: este
                         plugin estaba declarado SOLO con el goal `properties` (que existe
                         para exponer ${org.mockito:mockito-core:jar} al argLine de
                         surefire), y ningun perfil corria `analyze` ni `analyze-only`. Es
                         decir, nada comparaba nunca lo declarado arriba con lo que el
                         bytecode usa de verdad.

                         REPORTA, NO ROMPE — y es deliberado, escrito aqui para que se vea.
                         En un proyecto Spring Boot este analisis nace con decenas de
                         hallazgos que NO son defectos: los ~18 starters son POM agregadores
                         sin una sola clase propia, asi que el bytecode nunca los referencia
                         y salen todos como "Unused declared"; y al reves, cada import de
                         spring-web, hibernate-core, jackson o spring-security-web sale como
                         "Used undeclared" porque llega transitivamente por su starter.
                         Arrancar con failOnWarning=true deja el build rojo por ruido, y un
                         gate que nadie puede poner en verde acaba desactivado — es
                         exactamente el error que ya se cometio con jacoco.line.minimum en
                         0.90 y que el comentario de esa propiedad documenta.

                         COMO SE CIERRA (issue abierto): correr `mvn verify` una vez, leer
                         la lista real, enumerar los starters legitimos UNO A UNO en
                         <ignoredUnusedDeclaredDependencies> y solo entonces subir
                         failOnWarning a true. Nunca con un comodin
                         org.springframework.boot:spring-boot-starter-*: ese comodin habria
                         tapado tambien a spring-boot-starter-mail, o sea al unico caso que
                         este gate existe para atrapar. -->
                    <execution>
                        <id>analyze-declared-dependencies</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>analyze-only</goal>
                        </goals>
                        <configuration>
                            <failOnWarning>false</failOnWarning>
                            <!-- Los scopes runtime/test/provided no los ve el analisis de
                                 bytecode de compilacion por definicion: sin esto,
                                 mysql-connector-j, jjwt-impl, jjwt-jackson y todo el arbol
                                 de test saldrian como "no usadas" siendo correctas. -->
                            <ignoreNonCompile>true</ignoreNonCompile>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <!-- Comillas obligatorias: sin ellas surefire parte el argumento en
                         los espacios de la ruta del .m2 y el fork muere con
                         "Error opening zip file or JAR manifest missing".

                         @{argLine} es sustitucion TARDIA: resuelve la propiedad argLine en
                         el momento de ejecutar, que es cuando jacoco:prepare-agent ya la ha
                         reescrito con su -javaagent. Por eso conviven los dos agentes y por
                         eso la propiedad argLine se declara vacia arriba: sin declararla,
                         @{argLine} quedaria literal cuando jacoco no corre.

                         -Xmx explicito porque sin el la JVM del fork se queda con el default de
                         ~25% de la RAM del runner (unos 4 GB de los 16 GB de ubuntu-24.04) y eso no
                         da para la suite: el volcado a disco esta para que un OOM futuro deje
                         evidencia y no un stack sin contexto. maxSize baja de 32 a 8 porque retener
                         contextos solo compensa si se reutilizan, y aqui las rodajas @WebMvcTest
                         comparten WebMvcSliceConfig: con 8 la cache sigue acertando y se deja de
                         pagar memoria por el resto.

                         EL -Xmx Y EL maxSize SON POR FORK, y desde forkCount=2 (ver la ejecucion
                         default-test) hay DOS. La aritmetica, escrita para que se pueda rehacer:
                         2 x 4 GB = 8 GB de techo sobre los 16 GB del runner, y el techo NO es
                         reserva -la JVM compromete perezosamente-, asi que la huella real es la de
                         hoy repartida en dos procesos. Se mantiene en 4 GB A PROPOSITO en vez de
                         bajarlo a 3: 4 GB es el unico valor con el que se ha visto pasar la suite
                         ENTERA, y cada fork recibe ahora la mitad de las clases, de modo que el
                         mismo techo es estrictamente mas holgado que antes. Bajarlo seria cambiar
                         dos variables a la vez y estrenar un numero que nadie ha medido.

                         El maxSize=8 se queda igual por el mismo razonamiento, y no por inercia:
                         lo que llena esa cache es el numero de configuraciones de contexto
                         DISTINTAS que ve el fork, y al repartir las clases entre dos forks ese
                         numero baja. Con 8 por fork la tasa de acierto no puede empeorar respecto
                         al fork unico. Si algun dia aparece un OOM aqui, el primer dial es este
                         maxSize (8 -> 6), NUNCA el de failsafe: alli el 4 es un ajuste de
                         fiabilidad y bajarlo SUMA ~85 s (ver #284 mas abajo).

                         HeapDumpPath lleva ${surefire.forkNumber} porque los dos forks escribirian
                         si no en el MISMO fichero: el segundo OOM pisaria la evidencia del primero,
                         o los dos volcados se entrelazarian en un .hprof ilegible. Justo el dia que
                         hace falta. Failsafe usa su propio nombre por lo mismo. -->
                    <!-- Ver `skipUTs` en <properties>: por defecto false, o sea surefire
                         corre. Va a nivel de PLUGIN para que alcance a las DOS ejecuciones
                         -default-test y archunit-tests-: si solo cubriera la primera, el job
                         de integracion seguiria pagando las reglas de ArchUnit. -->
                    <skip>${skipUTs}</skip>
                    <argLine>@{argLine} -Xmx4g -XX:+HeapDumpOnOutOfMemoryError -XX:HeapDumpPath="${project.build.directory}/heapdump-surefire-${surefire.forkNumber}.hprof" -Dspring.test.context.cache.maxSize=8 -javaagent:"${org.mockito:mockito-core:jar}"</argLine>
                    <environmentVariables>
                        <!-- EncryptedStringConverter lo instancia Hibernate, no Spring, asi
                             que lee la clave del ENTORNO de forma estatica y sin default: sin
                             esto no arranca ningun contexto con JPA. Clave de juguete, valida
                             solo para que el converter se inicialice en tests; jamas se usa
                             fuera de aqui y no descifra nada real. -->
                        <DIAN_ENC_KEY>cvPOiufeDsByvGdw71gg8XxS880Xf7S6lH9WvdfiwQY=</DIAN_ENC_KEY>
                    </environmentVariables>
                </configuration>
                <!-- DOS EJECUCIONES, Y LA SEGUNDA EXISTE POR UN MOTIVO DE CORRECCION, NO DE
                     VELOCIDAD. Medido: surefire son 228 s de los 663 s de `mvn verify`, en un
                     unico fork y en serie. Repartir las clases en dos JVM lo baja a ~130 s.

                     POR QUE forkCount=2 ES SEGURO EN GENERAL. Es paralelismo de PROCESO, no de
                     hilo: dentro de cada fork las clases siguen ejecutandose una detras de otra
                     (no se toca `parallel` ni `threadCount`). Por eso todo el estado global de
                     JVM sigue estando tan aislado como hoy — cada fork tiene su propio
                     LoggerContext de Logback, su SecurityContextHolder, sus caches estaticas y su
                     Locale por defecto. Eso importa aqui: el issue #277 documenta seis clases que
                     enganchan sumideros al LoggerContext global y dos pares que compiten por el
                     mismo logger; con hilos serian intermitentes, con forks NO se tocan. Lo unico
                     que forkCount=2 pone en riesgo es el estado compartido FUERA de la JVM.

                     Auditado el arbol de test entero (1.673 ficheros) buscando justo eso: cero
                     @TempDir, cero ficheros temporales, cero System.setProperty, cero
                     Locale/TimeZone.setDefault, cero puertos a la escucha, cero Testcontainers en
                     alcance de surefire (AbstractDataJpaTest es abstract y sus hijas son *IT, que
                     corren en failsafe) y un unico campo static mutable, privado de su clase
                     (LogbackRedactionConfigTest). Solo aparecio UN recurso externo compartido, y
                     es el de la ejecucion de abajo.

                     LA EXCEPCION: EL STORE DE ARCHUNIT (issue #280). HexagonalArchitectureTest y
                     PiramideDeTestsTest escriben las dos en config/archunit/violation-store, un
                     directorio de ficheros que FreezingArchRule reescribe cuando una regla
                     encuentra menos violaciones de las registradas. Los ficheros UUID por regla
                     son disjuntos, pero `stored.rules` -el indice descripcion -> UUID- es UNO
                     SOLO. Si las dos clases caen en forks distintos y las dos lo reescriben, se
                     pierde una entrada: la regla que se queda sin foto cuenta su deuda historica
                     entera como violacion NUEVA y el build se pone rojo sin que el diff del store
                     lo explique — o, al reves, deuda ya resuelta vuelve a entrar en silencio.
                     Sobre el gate que sostiene las veinticinco reglas de CLAUDE.md.

                     Y OJO: el arreglo que propone el #280 -@ResourceLock o
                     @Execution(SAME_THREAD)- NO sirve para esto. Esas son primitivas de JUnit para
                     coordinar HILOS dentro de una misma JVM; entre dos procesos no coordinan nada.
                     La unica forma de garantizarlo es que las dos clases compartan fork, y eso es
                     lo que hace la ejecucion `archunit-tests`.

                     De regalo arregla el tercer punto del #280: las dos @AnalyzeClasses importan
                     el bytecode con opciones distintas (una con DoNotIncludeTests, otra sin), asi
                     que son dos importaciones completas de com.vetsoftware.app. En forks distintos
                     estarian vivas A LA VEZ contra 8 GB de techo; en el mismo fork van una detras
                     de otra y la primera se puede recolectar.

                     Coste de aislarlas: 14,4 s medidos en local (8,7 + 5,7) y 28,5 s en CI segun
                     el #280. Es lo unico que queda en serie, y sale a cuenta: ~(228-28,5)/2 + 28,5
                     + arranque de una JVM extra sigue estando en el entorno de los 130 s.

                     LAS DOS EJECUCIONES NO SE PISAN, y conviene decir por que: el comentario de
                     failsafe de mas abajo cuenta que surefire cachea por configuracion efectiva y
                     que dos ejecuciones gemelas se saltan la segunda. Aqui las configuraciones
                     DIFIEREN -una excluye lo que la otra incluye-, asi que las dos corren. Es
                     justo el efecto que alli era un accidente y aqui es el diseno. Escriben en el
                     mismo target/surefire-reports pero sobre clases disjuntas: ni un TEST-*.xml
                     compartido. -->
                <executions>
                    <execution>
                        <!-- La ejecucion que el ciclo de vida ya declara. Solo se le anade el
                             reparto en dos forks y la exclusion de las dos clases de ArchUnit. -->
                        <id>default-test</id>
                        <configuration>
                            <forkCount>2</forkCount>
                            <reuseForks>true</reuseForks>
                            <excludes>
                                <!-- Surefire pierde sus exclusiones por defecto en cuanto el POM
                                     declara <excludes>, y **/*$* es la que evita que una clase
                                     anidada se tome por clase de test. Se repite a mano. -->
                                <exclude>**/*$*</exclude>
                                <exclude>**/architecture/HexagonalArchitectureTest.java</exclude>
                                <exclude>**/architecture/PiramideDeTestsTest.java</exclude>
                            </excludes>
                        </configuration>
                    </execution>
                    <execution>
                        <!-- Las dos clases que comparten el store de ArchUnit, juntas y en un solo
                             fork. Si algun dia nace una tercera clase con @AnalyzeClasses y
                             freeze(...), va AQUI: dejarla en el reparto reabre el #280. -->
                        <id>archunit-tests</id>
                        <phase>test</phase>
                        <goals>
                            <goal>test</goal>
                        </goals>
                        <configuration>
                            <forkCount>1</forkCount>
                            <!-- <test> Y NO <includes>, Y ESTO IMPORTA. Medido: -Dtest de la linea
                                 de comandos IGNORA tanto <includes> como <excludes>. Con
                                 <includes>, un `mvn test -Dtest=AnimalTest` corria AnimalTest en
                                 las DOS ejecuciones -28 tests, dos veces, comprobado-, que es
                                 exactamente el "dos veces en silencio" contra el que avisa el
                                 comentario de failsafe. Un parametro configurado explicitamente en
                                 el POM gana a la propiedad de usuario del mismo nombre, asi que
                                 <test> clava estas dos clases pase lo que pase en la linea de
                                 comandos y la duplicacion desaparece.

                                 Efecto que queda, dicho para que nadie lo descubra a solas: un
                                 `mvn test -Dtest=LoQueSea` paga ademas los ~14 s de las reglas de
                                 arquitectura, y el `mvn test -Dtest='HexagonalArchitectureTest,
                                 PiramideDeTestsTest'` que documenta CLAUDE.md las corre dos veces
                                 -una por ejecucion- porque ahi -Dtest se salta el <excludes> de
                                 default-test. Sigue siendo correcto y verde, solo mas lento en un
                                 comando que se usa a mano y de tarde en tarde. -->
                            <test>HexagonalArchitectureTest, PiramideDeTestsTest</test>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-failsafe-plugin</artifactId>
                <configuration>
                    <!-- Las rodajas @DataJpaTest levantan un MySQL real con Testcontainers:
                         se llaman *IT y corren en `mvn verify`, no en `mvn test`. Asi el
                         ciclo local rapido no exige Docker, pero nada se mergea sin que las
                         migraciones y las consultas se hayan probado contra el motor de
                         verdad. Sin este plugin las clases *IT no las ejecutaba NADIE:
                         surefire solo recoge *Test/*Tests.

                         @{failsafeArgLine} lo escribe jacoco:prepare-agent-integration, de
                         modo que la cobertura de estas pruebas tambien cuenta (ver el merge
                         de los dos .exec mas abajo). El agente de Mockito va igual que en
                         surefire, con la ruta entrecomillada por los espacios del .m2.

                         -Xmx explicito por lo mismo que en surefire, pero aqui ya medido: el fork
                         moria con OutOfMemoryError tras 314 s y solo 203 de las ~1.130 pruebas
                         que habia entonces (BE-COV, 2026-08-17; hoy son 1.069).

                         maxSize=4 son los 3 contextos que sobreviven al colapso de las rodajas
                         mas uno de margen, y es un ajuste de FIABILIDAD, no de velocidad (#284).
                         Los tres estan medidos, no estimados: (1) @DataJpaTest +
                         PersistenceSliceConfig, la clave que comparten las 90 rodajas de
                         persistencia; (2) @DataJpaTest pelado, que es solo SchemaMigrationsIT y
                         se deja fuera del bloque A PROPOSITO -es el canario de "las migraciones
                         aplican sobre un contexto minimo", y meterlo dentro lo pondria rojo por
                         cualquier bean ajeno, perdiendo justo esa senal-; y (3) @SpringBootTest,
                         que comparten OpenApiContractIT y TraceparentPropagationIT.

                         Por que NO puede quedarse en 2: con 2 la LRU no puede retener las tres.
                         Expulsa una segun el orden de ejecucion y, si la expulsada resulta ser la
                         de las 90 rodajas, son ~85 s que reaparecen de forma intermitente y EN
                         VERDE. Ese es justo el coste que nadie llega a diagnosticar, porque no
                         rompe nada: comportamiento variable disfrazado de build correcto.

                         El argumento de memoria que justificaba el 2 sigue siendo valido en su
                         forma pero ya no en su numero. Se escribio cuando cada *IT declaraba su
                         propio @Import y la cache no acertaba NUNCA -92 contextos pedidos de uno
                         en uno, cada uno arrastrando los metadatos Hibernate de las 104
                         *JpaEntity-. Hoy acierta 87 de 90 veces, y con 3 contextos vivos en vez
                         de 92 retener 4 es barato. Si vuelves a leer esto y las rodajas han
                         dejado de compartir PersistenceSliceConfig, el numero de aqui es lo
                         primero que hay que recalcular.

                         SIN <executions> PROPIAS, A PROPOSITO. spring-boot-starter-parent:4.1.0
                         ya declara en su pluginManagement (lineas 118-130 de su POM) una
                         ejecucion (default) de failsafe con los goals integration-test + verify.
                         Esta <configuration> esta a nivel de PLUGIN, no de ejecucion, asi que se
                         le aplica igual; la prueba es que target/jacoco-it.exec pesa ~2,9 MB, y
                         el agente de JaCoCo solo pudo engancharse al fork por el argLine de aqui.

                         Aqui vivio ademas una segunda ejecucion, <id>integration-tests</id>, con
                         esos mismos dos goals. NO se ejecutaba dos veces, pero solo por accidente:
                         surefire cachea por configuracion efectiva y las dos claves coincidian
                         ("Skipping execution of surefire because it has already been run for this
                         configuration", dos banners T E S T S, 93 TEST-*.xml en failsafe-reports y
                         no 186). El dia que una de las dos hubiera recibido un <includes>, un
                         <groups> o un <systemPropertyVariables>, las claves habrian dejado de
                         coincidir y las 93 rodajas *IT se habrian ejecutado DOS VECES en silencio:
                         +14 min, y una segunda pasada sobre una base ya usada, que es justo como
                         se inventan o se enmascaran las dependencias de orden entre pruebas.

                         Tampoco se pone <skip> en la del padre: esa es la que arbitra el build
                         -cuando fallo, fallo con "Failed to execute goal ...failsafe...:verify
                         (default)"-. Si algun dia hacen falta parametros distintos para las *IT,
                         van en esta <configuration> de plugin, nunca en una ejecucion gemela. -->
                    <!-- SE QUEDA EN UN SOLO FORK, A PROPOSITO. Las rodajas *IT comparten el
                         contenedor MySQL singleton de AbstractDataJpaTest y la misma base: dos
                         forks serian dos suites escribiendo en las mismas tablas, y el resultado
                         no es un fallo limpio sino datos que aparecen y desaparecen entre pruebas.
                         Paralelizar esto es otro problema -pide una base por fork- y no se aborda
                         aqui. Lo unico que cambia es el nombre del volcado, que antes colisionaba
                         con el de surefire. -->
                    <argLine>@{failsafeArgLine} -Xmx4g -XX:+HeapDumpOnOutOfMemoryError -XX:HeapDumpPath="${project.build.directory}/heapdump-failsafe.hprof" -Dspring.test.context.cache.maxSize=4 -javaagent:"${org.mockito:mockito-core:jar}"</argLine>
                    <environmentVariables>
                        <!-- Mismo motivo que en surefire: EncryptedStringConverter lo
                             instancia Hibernate y lee la clave del entorno sin default. -->
                        <DIAN_ENC_KEY>cvPOiufeDsByvGdw71gg8XxS880Xf7S6lH9WvdfiwQY=</DIAN_ENC_KEY>
                    </environmentVariables>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>${jacoco-maven-plugin.version}</version>
                <configuration>
                    <excludes>
                        <!-- Sin logica propia: solo estructura o arranque. Medirlos infla
                             la cobertura sin decir nada sobre el riesgo real.

                             El criterio para admitir un glob nuevo, que es el que dejo
                             fuera a los candidatos evidentes (excepciones, enums,
                             *Properties, los *Ref del dominio): se leen TODAS las clases
                             que el glob captura, y si una sola tiene una rama, un throw,
                             una factoria o una traduccion, el glob se rechaza. Medido: las
                             exclusiones "obvias" que se descartaron por eso BAJABAN la
                             cifra, porque retiraban mas linea cubierta que sin cubrir. -->
                        <!-- Arranque. Ojo a la S de Software: hasta 2026-08-17 este glob
                             decia VetsoftwareApplication y no casaba con la clase real
                             (com.vetsoftware.app.VetSoftwareApplication), asi que la
                             exclusion declarada no se aplicaba. -->
                        <exclude>**/VetSoftwareApplication.class</exclude>
                        <exclude>**/*JpaEntity.class</exclude>
                        <!-- #126. Aqui habia un `**/config/**` que se llevaba las ONCE
                             clases de */config/* -760 lineas- de un tiron, incumpliendo el
                             criterio escrito justo arriba: bastaba con leerlas para
                             rechazarlo. SecurityConfig decide quien entra sin autenticarse
                             y tiene rama y throw; PublicRoutes ES la lista de rutas
                             abiertas, con un throw en el constructor compacto de Route; y
                             ActuatorSecurityProperties.validate lanza tres veces. Medir en
                             cero la clase que declara que rutas no piden JWT es justo lo
                             contrario de lo que el suelo protege.

                             Aplicado el criterio clase a clase, quedan fuera SOLO estas
                             cuatro y vuelven al denominador las otras siete
                             (SecurityConfig, PublicRoutes, ActuatorSecurityConfig,
                             ActuatorSecurityProperties, AsyncConfig, ObservabilityConfig y
                             RemoteConnectionValidator).

                             CacheConfig, ClockConfig y SchedulingConfig son cableado puro:
                             ni una rama, ni un throw, ni una traduccion. Un @Bean que
                             devuelve Clock.systemDefaultZone(), o una clase vacia con
                             @EnableScheduling, solo se pueden "probar" con un isNotNull()
                             — cobertura sin señal, que es la forma de enterrar la de
                             verdad.

                             RateLimitConfig es el caso incomodo y se deja escrito en vez de
                             disimulado: SI tiene una rama —el ternario url-vacia vs
                             host+port de rateLimitRedisClient— pero RedisClient no expone
                             la RedisURI con la que se construyo, asi que el ternario no se
                             puede afirmar desde fuera, y sus otros dos beans abren conexion
                             real contra Redis. Cubrirlo pide extraer la resolucion de la
                             URI a un metodo propio; eso es un cambio de src/main y va en su
                             issue, no en un exclude silencioso. -->
                        <exclude>**/infrastructure/config/CacheConfig.class</exclude>
                        <exclude>**/infrastructure/config/RateLimitConfig.class</exclude>
                        <exclude>**/infrastructure/config/ClockConfig.class</exclude>
                        <exclude>**/infrastructure/config/SchedulingConfig.class</exclude>
                        <exclude>**/*Request.class</exclude>
                        <!-- La forma del JSON de salida. El paquete entero, no solo el
                             sufijo *Response: dentro solo viven *Response y los 136
                             *Summary companion (comprobado: ni un fichero mas, ni un
                             throw, ni un return, ni una factoria, cero ramas). El glob
                             por paquete ademas alcanza a los records anidados
                             (AccountsPayableAgingResponse$Bucket), que **/*Response.class
                             dejaba midiendo.

                             Y tiene que ir anclado al paquete: **/*Summary*.class se
                             tragaria los 134 *SummaryDto de application/dto, que SI son
                             logica de proyeccion (el from(...) decide que campos salen). -->
                        <exclude>**/*Response.class</exclude>
                        <exclude>**/infrastructure/web/response/*.class</exclude>
                    </excludes>
                </configuration>
                <executions>
                    <execution>
                        <id>prepare-agent</id>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                    </execution>
                    <execution>
                        <!-- Segundo agente, para las rodajas *IT de failsafe. Escribe en
                             target/jacoco-it.exec y expone su -javaagent en la propiedad
                             failsafeArgLine, que el argLine de failsafe resuelve tarde. -->
                        <id>prepare-agent-integration</id>
                        <goals>
                            <goal>prepare-agent-integration</goal>
                        </goals>
                        <configuration>
                            <propertyName>failsafeArgLine</propertyName>
                        </configuration>
                    </execution>
                    <!-- Aqui vivia una ejecucion <id>report</id> en fase `test`, descrita como
                         "informe de `mvn test`: solo pruebas unitarias". Se retira porque su
                         salida la PISA entera `report-all`: las dos usan el outputDirectory por
                         defecto -target/site/jacoco- y report-all corre despues, en
                         post-integration-test, sobre el .exec fusionado de surefire + failsafe. En
                         un `mvn verify` el informe que sobrevivia era siempre el segundo, asi que
                         el primero era ~2 s de analizar 2.123 clases para escribir ficheros que
                         nadie llegaba a leer. Comprobado que no lo consume nadie: ni un workflow
                         de .github, ni el Dockerfile, ni un script del repo mencionan jacoco.

                         LO QUE SI CAMBIA, dicho sin disimulo: `mvn test` a secas ya no deja
                         target/site/jacoco/index.html. El suelo de cobertura no se toca -lo miden
                         report-all y check-floor sobre jacoco-all.exec, los dos en
                         post-integration-test- pero CLAUDE.md documenta ese HTML en su seccion
                         "Commands" y en "Cobertura". Para el informe local sigue estando
                         `mvn jacoco:report` tras el `mvn test`, o `mvn verify`. Registrado en su
                         issue para que la documentacion no se quede mintiendo. -->
                    <execution>
                        <!-- Une lo que midio surefire con lo que midio failsafe. Si las *IT
                             se saltan (-DskipITs) el segundo .exec no existe y el merge se
                             queda con el primero, asi que el suelo sigue siendo medible. -->
                        <id>merge-unit-and-integration</id>
                        <phase>post-integration-test</phase>
                        <goals>
                            <goal>merge</goal>
                        </goals>
                        <configuration>
                            <fileSets>
                                <fileSet>
                                    <directory>${project.build.directory}</directory>
                                    <includes>
                                        <include>jacoco.exec</include>
                                        <include>jacoco-it.exec</include>
                                    </includes>
                                </fileSet>
                            </fileSets>
                            <destFile>${project.build.directory}/jacoco-all.exec</destFile>
                        </configuration>
                    </execution>
                    <execution>
                        <!-- Reemplaza el informe del `test` con el de las dos fuentes. Va
                             declarado ANTES de check-floor: dentro de la misma fase, Maven
                             respeta el orden de declaracion. -->
                        <!-- Sube a post-integration-test junto con check-floor, por el motivo que
                             se explica alli. Siguen en la misma fase y en el mismo orden. -->
                        <id>report-all</id>
                        <phase>post-integration-test</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                        <configuration>
                            <dataFile>${project.build.directory}/jacoco-all.exec</dataFile>
                        </configuration>
                    </execution>
                    <execution>
                        <!-- Trinquete: el suelo global no puede bajar. Se sube a mano al
                             valor medido cuando un PR lo supera; nunca se baja. -->
                        <!-- post-integration-test y no verify, que es donde vivia. Quien EJECUTA
                             las *IT es `failsafe:integration-test`, una fase antes; quien las
                             hace fallar es `failsafe:verify`, que solo lee failsafe-summary.xml.
                             Colgando de post-integration-test los datos ya estan completos -el
                             merge de arriba corre en esta misma fase y va declarado antes- y
                             ademas la cobertura se mide AUNQUE una *IT este en rojo, que antes no
                             pasaba: el build moria en `failsafe:verify` y el suelo no se
                             comprobaba nunca.

                             Esto NO invierte el problema. El detalle del fallo de una *IT ya se
                             imprime durante `integration-test`, miles de lineas antes; lo unico
                             que hace `failsafe:verify` es levantar el error. La cobertura, en
                             cambio, no se imprime en ningun otro sitio: `jacoco:report` solo
                             escribe ficheros, y el porcentaje solo sale por `jacoco:check`.
                             Contrapartida real y aceptada: si fallan cobertura Y una *IT, el
                             ultimo [ERROR] del log habla de cobertura y el de failsafe ya no se
                             emite -el resultado sigue en target/failsafe-reports-. -->
                        <id>check-floor</id>
                        <phase>post-integration-test</phase>
                        <goals>
                            <goal>check</goal>
                        </goals>
                        <configuration>
                            <!-- Ver `skipCoverageCheck` en <properties>. Por defecto false:
                                 un `mvn verify` completo -local, o el de cualquier otro
                                 workflow- comprueba el suelo igual que siempre. Solo el job
                                 `integration` de ci.yml lo pone en true, porque alli el
                                 .exec de las unitarias no existe y medir seria mentir. El
                                 sitio donde se mide con los dos .exec delante es el job
                                 `coverage-gate`, que ademas comprueba ANTES que los dos
                                 ficheros llegaron: ver "Guarda anti-falso-verde" en ci.yml. -->
                            <skip>${skipCoverageCheck}</skip>
                            <dataFile>${project.build.directory}/jacoco-all.exec</dataFile>
                            <rules>
                                <rule>
                                    <element>BUNDLE</element>
                                    <limits>
                                        <limit>
                                            <counter>LINE</counter>
                                            <value>COVEREDRATIO</value>
                                            <minimum>${jacoco.line.minimum}</minimum>
                                        </limit>
                                    </limits>
                                </rule>
                            </rules>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>com.diffplug.spotless</groupId>
                <artifactId>spotless-maven-plugin</artifactId>
                <version>${spotless-maven-plugin.version}</version>
                <configuration>
                    <java>
                        <eclipse>
                            <version>${eclipse-jdt-formatter.version}</version>
                            <file>${project.basedir}/config/spotless/eclipse-formatter.properties</file>
                        </eclipse>
                        <removeUnusedImports/>
                        <formatAnnotations/>
                    </java>
                    <!-- El indice de up-to-date fuera de target/. Por defecto vive en
                         target/spotless-index, asi que `mvn clean` lo borra y spotless vuelve a
                         leer y formatear los 5.596 ficheros desde cero en cada pasada local
                         ("Index file does not exist. Fallback to an empty index" en el log;
                         17,7 s medidos en CI). En .build-cache/ sobrevive al clean.

                         <enabled>true</enabled> es OBLIGATORIO aunque el defecto del plugin sea
                         "activado": AbstractSpotlessMojo inicializa el campo con
                         UpToDateChecking.enabled(), pero en cuanto el POM declara el elemento,
                         Plexus construye un objeto NUEVO con el constructor por defecto
                         (enabled = false) y solo rellena los hijos listados. Poner solo
                         <indexFile> apagaria la comprobacion entera en vez de moverla.

                         Esto NO relaja ningun control: el indice guarda una huella del plugin
                         -version + configuracion + los formatters- y si cambia descarta el indice
                         completo ("Index file corresponds to a different configuration of the
                         plugin"), asi que tocar config/spotless/* obliga a reformatear todo.
                         Por fichero compara la fecha de modificacion. En CI el runner es efimero
                         y sigue pagando la pasada fria entera. -->
                    <upToDateChecking>
                        <enabled>true</enabled>
                        <indexFile>${project.basedir}/.build-cache/spotless-index</indexFile>
                    </upToDateChecking>
                </configuration>
                <executions>
                    <execution>
                        <!-- process-sources y no verify, que es donde vivia. Es un control de
                             FUENTE: no necesita bytecode ni tests. En verify no se ejecutaba
                             hasta el minuto 11:31 de un build de 11:45 (medido, no estimado), y
                             sobre todo: si una *IT fallaba, `failsafe:verify` abortaba antes y
                             este gate NO LLEGABA A CORRER. Un PR con un fallo de formato y otro
                             de contrato necesitaba dos ciclos rojos de once minutos para
                             enterarse de los dos. Aqui falla en segundos.

                             Va DESPUES de validate, que es donde vive el enforcer: si el arbol de
                             dependencias no es reproducible, eso se dice antes que un espacio de
                             mas. Y va ANTES de compile, asi que no paga ni la compilacion.

                             Efecto lateral asumido y deliberado: `mvn test` tambien lo ejecuta
                             ahora, porque process-sources es anterior a test. Es coherente con el
                             pre-commit, que ya exige spotless y checkstyle sobre lo staged: si
                             esto te bloquea un `mvn test`, tampoco habrias podido commitear. -->
                        <id>spotless-check</id>
                        <phase>process-sources</phase>
                        <goals>
                            <goal>check</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-checkstyle-plugin</artifactId>
                <version>${maven-checkstyle-plugin.version}</version>
                <configuration>
                    <!-- Mismo motivo que el indice de spotless: el defecto es
                         target/checkstyle-cachefile, que `mvn clean` borra, y checkstyle vuelve a
                         analizar los 5.596 ficheros desde cero (15,8 s medidos en CI). En
                         .build-cache/ sobrevive al clean.

                         Tampoco relaja el control: PropertyCacheFile guarda un SHA-1 del objeto
                         Configuration efectivo bajo la clave "configuration" y, si no coincide al
                         cargar, hace reset() y tira la cache entera; ademas invalida por los
                         recursos externos que la configuracion referencia. Es decir, tocar
                         config/checkstyle/* obliga a reanalizarlo todo. Por fichero compara la
                         fecha de modificacion. -->
                    <cacheFile>${project.basedir}/.build-cache/checkstyle-cachefile</cacheFile>
                    <configLocation>config/checkstyle/checkstyle.xml</configLocation>
                    <includeTestSourceDirectory>true</includeTestSourceDirectory>
                    <includeResources>false</includeResources>
                    <includeTestResources>false</includeTestResources>
                    <excludeGeneratedSources>true</excludeGeneratedSources>
                    <consoleOutput>true</consoleOutput>
                    <failOnViolation>true</failOnViolation>
                    <violationSeverity>warning</violationSeverity>
                    <maxAllowedViolations>0</maxAllowedViolations>
                </configuration>
                <dependencies>
                    <dependency>
                        <groupId>com.puppycrawl.tools</groupId>
                        <artifactId>checkstyle</artifactId>
                        <version>${checkstyle.version}</version>
                    </dependency>
                </dependencies>
                <executions>
                    <execution>
                        <!-- Misma fase y mismo motivo que spotless-check: es un control de
                             FUENTE, no necesita bytecode, y colgado de verify quedaba detras de
                             `failsafe:verify` Y de spotless, asi que hacia falta un TERCER ciclo
                             rojo para llegar a verlo. Corre despues de spotless porque este
                             plugin se declara despues: dentro de una misma fase, Maven respeta el
                             orden de declaracion de los plugins. -->
                        <id>checkstyle-check</id>
                        <phase>process-sources</phase>
                        <goals>
                            <goal>check</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
            <!-- BE-27. Higiene de dependencias verificable en el build, no por convencion.
                 banDynamicVersions es el equivalente realista al dependency locking en Maven:
                 no existe un lockfile, pero si una version declarada deja de ser exacta (un
                 rango [1.0,2.0), un LATEST, un RELEASE) el arbol resuelto deja de ser
                 reproducible y el SBOM que se publica en la release describe un artefacto
                 distinto del que se construyo. requireReleaseDeps cierra la otra mitad: un
                 SNAPSHOT en el arbol es contenido mutable dentro de una imagen inmutable. -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-enforcer-plugin</artifactId>
                <version>${maven-enforcer-plugin.version}</version>
                <executions>
                    <execution>
                        <id>enforce-dependency-hygiene</id>
                        <phase>validate</phase>
                        <goals>
                            <goal>enforce</goal>
                        </goals>
                        <configuration>
                            <rules>
                                <!-- Recorre el arbol resuelto completo, incluidas las transitivas:
                                     una version dinamica heredada de un tercero rompe la
                                     reproducibilidad igual que una declarada aqui. -->
                                <banDynamicVersions>
                                    <allowSnapshots>false</allowSnapshots>
                                    <allowLatest>false</allowLatest>
                                    <allowRelease>false</allowRelease>
                                    <allowRanges>false</allowRanges>
                                </banDynamicVersions>
                                <requireReleaseDeps>
                                    <message>Ninguna dependencia SNAPSHOT: la imagen es inmutable y su arbol tambien.</message>
                                </requireReleaseDeps>
                                <requireJavaVersion>
                                    <version>[25,26)</version>
                                    <message>El proyecto compila con Java 25 (ver java.version).</message>
                                </requireJavaVersion>
                            </rules>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <!-- BE-27. SBOM CycloneDX del artefacto REAL (lo que acaba en el jar), no del
                 tooling de npm. Se genera en 'package', de modo que cualquier 'mvn verify'
                 -local o de CI- deja target/bom.json listo: el escaneo de vulnerabilidades
                 del workflow de seguridad consume ese fichero y la release lo publica como
                 activo. Es la lista de materiales que hoy no existia y sin la cual no se
                 puede responder "estoy afectado por este CVE" sin reconstruir el proyecto. -->
            <plugin>
                <groupId>org.cyclonedx</groupId>
                <artifactId>cyclonedx-maven-plugin</artifactId>
                <version>${cyclonedx-maven-plugin.version}</version>
                <configuration>
                    <projectType>application</projectType>
                    <outputFormat>json</outputFormat>
                    <outputName>bom</outputName>
                    <!-- Solo lo que se despliega. Las dependencias de test no viajan en el
                         jar y meterlas produce alertas sobre codigo que nunca corre en
                         produccion, que es como se muere un gate de seguridad. -->
                    <includeTestScope>false</includeTestScope>
                    <!-- El SBOM no es un artefacto Maven que se instale ni se despliegue:
                         se retiene como artefacto de CI y como activo de la release. -->
                    <skipAttach>true</skipAttach>
                </configuration>
                <executions>
                    <!-- NEUTRALIZA EL SBOM HEREDADO. spring-boot-starter-parent:4.1.0 declara en su
                         pluginManagement (lineas 188-204 de su POM) una ejecucion SIN <id> -o sea
                         `default`- que engancha makeAggregateBom a generate-resources y escribe en
                         ${project.build.outputDirectory}/META-INF/sbom/application.cdx.json. El
                         resultado es que CADA build resolvia el arbol de dependencias y validaba un
                         BOM DOS VECES: esa y la `build-sbom` de aqui abajo. 9,7 s de CI por dos
                         listas del mismo arbol, ~5 s de ellos para la que nadie usa.

                         Verificado que no la consume nadie antes de apagarla, que era el riesgo
                         real:
                           - `grep -rn "META-INF/sbom|makeAggregateBom|application.cdx"` sobre
                             .github/, Dockerfile, .dockerignore, scripts/, docker/, deploy/ y
                             package.json: cero coincidencias.
                           - Los tres consumidores de SBOM del repo leen target/bom.json, que es la
                             salida de `build-sbom` y no la de esta: security.yml:136 lo genera con
                             `cyclonedx:makeBom` y lo escanea con Trivy (security.yml:167-184), y
                             publish-release.yml:376-397 lo publica como activo de la release.
                             (Numeros de linea reverificados el 2026-08-22: los anteriores
                             -security.yml:87 y :96-125, publish-release.yml:286-294- eran
                             correctos al escribirse y se desplazaron al insertarse los bloques de
                             cache de Maven, +51 lineas en security.yml y +104 en
                             publish-release.yml. Si vuelven a no cuadrar, busca por el nombre del
                             step, que es lo estable: "Generate CycloneDX SBOM", "Scan the SBOM for
                             known vulnerabilities" y "Attach the SBOM to the release".)
                           - El fichero de la heredada viajaba DENTRO del jar (target/classes/...),
                             donde lo leeria el endpoint /actuator/sbom de Actuator. Comprobado que
                             no esta expuesto en ningun perfil: application.yml:141-144 y
                             application-local.yml expone health,info,metrics,prometheus, y
                             application-dev.yml / application-prod.yml solo health. Ninguno incluye
                             `sbom`, asi que el endpoint no responde y el fichero embebido no lo lee
                             nadie.

                         Si algun dia se expone /actuator/sbom, hay que devolver esta ejecucion a
                         generate-resources: el jar dejo de traer su lista de materiales embebida. -->
                    <execution>
                        <id>default</id>
                        <phase>none</phase>
                    </execution>
                    <execution>
                        <id>build-sbom</id>
                        <phase>package</phase>
                        <goals>
                            <goal>makeBom</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
