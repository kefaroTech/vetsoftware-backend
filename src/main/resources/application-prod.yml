# Perfil prod — S3 real de AWS.
management:
  tracing:
    sampling:
      # Head-sampling al 100%: la app envía TODAS las trazas al otel-collector, que es quien
      # decide qué retener vía tail_sampling (errores + lentas + 10% del resto).
      # El muestreo real vive en docker/otel-collector.yml, no aquí.
      probability: ${TRACING_SAMPLING:1.0}

springdoc:
  api-docs:
    enabled: ${SPRINGDOC_API_DOCS_ENABLED:false}
  swagger-ui:
    enabled: ${SPRINGDOC_SWAGGER_UI_ENABLED:false}

vetsoftware:
  storage:
    s3:
      bucket: ${S3_BUCKET}
      region: ${S3_REGION:us-east-1}
      # sin endpoint override → SDK usa el endpoint real de AWS
      endpoint: ${S3_ENDPOINT:}
      # credenciales vacías → DefaultCredentialsProvider (IAM role / env vars / perfil)
      access-key: ${S3_ACCESS_KEY:}
      secret-key: ${S3_SECRET_KEY:}
      # virtual-hosted style (estándar en S3 real)
      path-style-access: ${S3_PATH_STYLE_ACCESS:false}
