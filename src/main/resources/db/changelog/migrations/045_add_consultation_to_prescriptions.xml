<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
                   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                   xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                   http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <changeSet id="045_add_consultation_to_prescriptions" author="orlando">
        <addColumn tableName="prescriptions">
            <column name="consultation_id" type="BIGINT">
                <constraints nullable="false" foreignKeyName="fk_prescriptions_consultation"
                             references="consultations(id)"/>
            </column>
        </addColumn>
        <!-- AJUSTE (primer rollback end-to-end de la cadena completa, nunca ejecutado hasta
             hoy): sin <rollback>, el dropColumn auto-generado no suelta la FK primero y
             revienta con "Cannot drop column ... needed in a foreign key constraint". -->
        <rollback>
            <dropForeignKeyConstraint baseTableName="prescriptions" constraintName="fk_prescriptions_consultation"/>
            <dropColumn tableName="prescriptions" columnName="consultation_id"/>
        </rollback>
    </changeSet>

</databaseChangeLog>
