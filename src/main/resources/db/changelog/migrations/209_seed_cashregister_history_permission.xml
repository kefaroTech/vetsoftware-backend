<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
                   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                   xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                   http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <!-- Permiso independiente para consultar el histórico de cajas y exportar sus arqueos. -->
    <changeSet id="209_cashregister_history_permission" author="orlando">
        <sql>
            INSERT INTO base_permissions (name, code, sub_module_id, enabled)
            SELECT 'Ver histórico de cajas', 'cashregister.history.read',
                   (SELECT id FROM sub_modules WHERE code = 'CASH'), 1
            WHERE NOT EXISTS (
                SELECT 1 FROM base_permissions WHERE code = 'cashregister.history.read');

            INSERT INTO base_role_permissions (base_role_id, base_permission_id, enabled)
            SELECT br.id, bp.id, 1
            FROM base_roles br
            JOIN base_permissions bp ON bp.code = 'cashregister.history.read'
            WHERE br.code = 'ADMIN'
              AND NOT EXISTS (
                  SELECT 1 FROM base_role_permissions brp
                  WHERE brp.base_role_id = br.id AND brp.base_permission_id = bp.id);

            INSERT INTO permissions (name, code, company_id, sub_module_id, enabled)
            SELECT bp.name, bp.code, c.id, bp.sub_module_id, 1
            FROM companies c
            CROSS JOIN base_permissions bp
            WHERE bp.code = 'cashregister.history.read'
              AND NOT EXISTS (
                  SELECT 1 FROM permissions p
                  WHERE p.company_id = c.id AND p.code = bp.code);

            INSERT INTO role_permissions (role_id, permission_id, enabled)
            SELECT r.id, p.id, 1
            FROM roles r
            JOIN permissions p ON p.company_id = r.company_id
                              AND p.code = 'cashregister.history.read'
            WHERE r.code = 'ADMIN'
              AND NOT EXISTS (
                  SELECT 1 FROM role_permissions rp
                  WHERE rp.role_id = r.id AND rp.permission_id = p.id);
        </sql>
        <rollback>
            DELETE FROM role_permissions
            WHERE permission_id IN (
                SELECT id FROM permissions WHERE code = 'cashregister.history.read');

            DELETE FROM permissions WHERE code = 'cashregister.history.read';

            DELETE FROM base_role_permissions
            WHERE base_permission_id IN (
                SELECT id FROM base_permissions WHERE code = 'cashregister.history.read');

            DELETE FROM base_permissions WHERE code = 'cashregister.history.read';
        </rollback>
    </changeSet>

</databaseChangeLog>
