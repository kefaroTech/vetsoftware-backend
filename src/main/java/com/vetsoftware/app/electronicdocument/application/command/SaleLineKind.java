package com.vetsoftware.app.electronicdocument.application.command;

/** Origen de una linea de venta POS: catalogo (producto/servicio) o item libre. */
public enum SaleLineKind {
    PRODUCT,
    SERVICE,
    GENERAL
}
