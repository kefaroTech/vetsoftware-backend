package com.vetsoftware.app.paymentgateway.domain;

/** Lo que el tenant puede ver del cobro del primer periodo. */
public enum FirstPeriodPaymentStatus {
    APPROVED, PENDING, DECLINED, NOT_ATTEMPTED
}
