package com.vetsoftware.app.subscription.domain;

/**
 * Tratamiento fiscal congelado en la linea.
 * {@code chk_subscription_items_tax_treatment}.
 */
public enum TaxTreatment {
    TAXED, EXEMPT, EXCLUDED
}
