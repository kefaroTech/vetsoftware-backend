@startuml prescription_delete
title DELETE /prescriptions/{id} — Delete Prescription

actor Client
participant "PrescriptionController" as Controller
participant "DeletePrescriptionUseCase\n<<port.in>>" as UseCase
participant "DeletePrescriptionService" as Service
participant "PrescriptionRepository\n<<port.out>>" as Repo
participant "PrescriptionJpaRepository\n<<spring-data>>" as JpaRepo
participant "MedicamentPrescriptionChildrenQueryPort\n<<port.out>>" as ChildrenQuery
participant "MedicamentPrescriptionJpaRepository\n<<spring-data>>" as ChildJpaRepo
database "MySQL" as DB
participant "GlobalExceptionHandler" as GEH

note over Client, GEH
  AuthFilter ya validó el JWT y puso EmployeeContext
  en SecurityContextHolder antes de llegar al controller.
end note

note over Client, DB
  Hibernate `@SoftDelete` filtra automáticamente:
  - DELETE → UPDATE enabled=false
  - Todo SELECT añade WHERE enabled=true
end note

Client -> Controller : DELETE /prescriptions/{id}
activate Controller

Controller -> UseCase : execute(id)
note right of UseCase
  @PreAuthorize:
  admin.all OR hasRole('SYSTEM')
end note
UseCase -> Service : execute(id)
activate Service

Service -> Repo : findById(id)
Repo -> JpaRepo : findById(id)
JpaRepo -> DB : SELECT prescription
DB --> JpaRepo : row | empty
JpaRepo --> Repo : Optional<PrescriptionJpaEntity>
alt prescription no existe
  Repo --> Service : Optional.empty()
  Service --> GEH : PrescriptionNotFoundException
  GEH --> Client : 404 Not Found
end
Repo --> Service : Optional<Prescription>

Service -> ChildrenQuery : existsActiveByPrescriptionId(id)
ChildrenQuery -> ChildJpaRepo : existsByPrescription_Id(id)
ChildJpaRepo -> DB : SELECT EXISTS(...) WHERE prescription_id=? AND enabled=true
DB --> ChildJpaRepo : true/false
ChildJpaRepo --> ChildrenQuery : boolean
ChildrenQuery --> Service : boolean
alt hay hijos activos
  Service --> GEH : PrescriptionHasActiveChildrenException
  GEH --> Client : 409 Conflict
end

Service -> Repo : delete(id)
Repo -> JpaRepo : deleteById(id)
JpaRepo -> DB : UPDATE prescription SET enabled=false WHERE id=?
DB --> JpaRepo : ok
JpaRepo --> Repo : void
Repo --> Service : void

Service --> UseCase : void
UseCase --> Controller : void
deactivate Service

Controller --> Client : 204 No Content
deactivate Controller

@enduml
